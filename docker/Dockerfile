# Use Ubuntu as the base image
FROM ubuntu:latest

# Install any basic dependencies if needed (e.g., curl, vim, etc.)
RUN apt-get update && apt-get install -y curl vim

# Set the default command to keep the container running in the background
CMD ["bash"]
